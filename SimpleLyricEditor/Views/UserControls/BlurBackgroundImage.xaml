<UserControl
    x:Class="SimpleLyricEditor.Views.UserControls.BlurBackgroundImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleLyricEditor.Views.UserControls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:behaviors="using:Microsoft.Toolkit.Uwp.UI.Animations.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Storyboard x:Name="FadeIn_Storyboard">
            <DoubleAnimation From="0" To="1" Duration="0:0:0.5" Storyboard.TargetName="image" Storyboard.TargetProperty="Opacity"/>
        </Storyboard>
        
        <Storyboard x:Name="FadeOut_Storyboard" Completed="{x:Bind SwitchImage}">
            <DoubleAnimation From="1" To="0" Duration="0:0:0.5" Storyboard.TargetName="image" Storyboard.TargetProperty="Opacity"/>
        </Storyboard>
    </UserControl.Resources>

    <Image x:Name="image" Source="{x:Bind ImageSource,Mode=OneTime}" Stretch="UniformToFill">
        <interactivity:Interaction.Behaviors>
            <behaviors:Blur Value="{x:Bind settings.BackgroundBlurDegree,Mode=OneWay}" Duration="0"/>
        </interactivity:Interaction.Behaviors>
    </Image>
</UserControl>
