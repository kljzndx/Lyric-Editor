<UserControl
    x:Class="SimpleLyricEditor.Views.UserControls.MultipleLyricPreview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleLyricEditor.Views.UserControls"
    xmlns:vm="using:SimpleLyricEditor.ViewModels"
    xmlns:temp="using:SimpleLyricEditor.Views.Template"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Rectangle Fill="{x:Bind Background,Mode=OneWay}"/>
        <ListView x:Name="Main_ListView" ItemsSource="{x:Bind LyricsList,Mode=OneWay}" SelectedItem="{x:Bind CurrentLyric}" SelectionChanged="Main_ListView_SelectionChanged" PointerEntered="Main_ListView_PointerEntered" PointerExited="Main_ListView_PointerExited">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:LyricItem">
                    <temp:MultipleLyricItemTemplate/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
