<UserControl
    x:Class="SimpleLyricEditor.Views.Template.ShortcutKeysItemTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleLyricEditor.Views.Template"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" RequestedTheme="Dark">

    <StackPanel Background="{x:Bind Background}" BorderBrush="{x:Bind BorderBrush}" BorderThickness="{x:Bind BorderThickness}">
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Border Background="Red" Margin="5,0">
                <TextBlock x:Uid="/ShortcutKeysDialog/Condition_TextBlock" Margin="5" FontWeight="Bold"/>
            </Border>
            <TextBlock Text="{x:Bind key.Condition}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="10,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Border Background="Green" Margin="5,0">
                <TextBlock x:Uid="/ShortcutKeysDialog/Function_TextBlock" Margin="5" FontWeight="Bold"/>
            </Border>
            <TextBlock Text="{x:Bind key.Function}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="10,0"/>
        </StackPanel>
    </StackPanel>
</UserControl>
