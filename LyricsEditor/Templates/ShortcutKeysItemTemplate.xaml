<UserControl
    x:Class="LyricsEditor.Templates.ShortcutKeysItemTemplate"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LyricsEditor.Templates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Margin="0,10" BorderThickness="3" BorderBrush="White">
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Border Background="OrangeRed" Padding="2" Margin="5,0">
                <TextBlock x:Uid="/ShortcutKeys/Conditions_TextBlock" FontWeight="Bold" Foreground="White"/>
            </Border>
            <TextBlock Text="{x:Bind stk.Condition}" Margin="5,0" Foreground="White" TextWrapping="Wrap"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="0,5">
            <Border Background="#ff009100" Padding="2" Margin="5,0">
                <TextBlock x:Uid="/ShortcutKeys/Function_TextBlock" FontWeight="Bold" Foreground="White"/>
            </Border>
            <TextBlock Text="{x:Bind stk.Function}" Margin="5,0" Foreground="White" TextWrapping="Wrap"/>
        </StackPanel>
    </StackPanel>
</UserControl>
